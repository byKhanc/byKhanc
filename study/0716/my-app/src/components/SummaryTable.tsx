type Complete = {
    user: string;
    car: string;
    date: string;
};

export default function SummaryTable({ user, car, data }: Complete) {
    return (
        <div>
            <h2>{user}</h2>
            
        </div>
    )